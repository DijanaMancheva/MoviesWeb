@model MoviesWeb.Models.User
@{
    ViewData["Title"] = "Reset Password";

}
<style>
    nav {
        background-color: url('https://www.popwebdesign.net/images/tutorial/photoshop/ps-menu_1.jpg');
    }

    body {
        background-image: url('https://www.zastavki.com/pictures/1600x900/2021Movies_Milina_character_of_the_movie_Mortal_Kombat_on_a_black_background_150945_25.jpg');
        background-size: cover; /* Ensures the image covers the entire background */
        background-repeat: no-repeat; /* Prevents the image from repeating */
        background-position: center; /* Centers the image in the viewport */
        height: 150vh; /* Ensures the body takes up the full viewport height */
        margin: 0; /* Removes any default margin */
        padding: 0; /* Removes any default padding */
    }

    .forma {
        display: flex;
        justify-content: center; /* Center horizontally */
        align-items: center; /* Center vertically */
        height: 100vh; /* Full viewport height */
        text-align: center; /* Center text inside form */
    }
</style>
<body>

    <h1 class="mb-4" style="text-align:center;color:red;font-style:italic;font-family: 'Lobster', cursive;"> Change password</h1>
    <hr />
    <div class="container">
        <form  asp-action="GetUserForChange" asp-controller="User" asp-route-id="@Model.Id">
            
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <h1>@ViewData["Title"]</h1>
            <div class="form-group form-check">
               @*  <input type="hidden" asp-for="@Model.Id" />
                <input type="hidden" asp-for="@Model.Active" />
                <input type="hidden" asp-for="@Model.Username" />
                <input type="hidden" asp-for="@Model.Phone" />
                <input type="hidden" asp-for="@Model.Name" />
                <input type="hidden" asp-for="@Model.Admin" />
                <input type="hidden" asp-for="@Model.Email" /> *@
                <div class="form-group">
                    <label asp-for="@Model.Id" class="control-label"></label>
                    <input asp-for="@Model.Id" class="form-control" />
                    <span asp-validation-for="@Model.Id" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Password" class="control-label" style="color:white"></label>
                    <input asp-for="@Model.Password" class="form-control" />
                    <span asp-validation-for="@Model.Password" class="text-danger"></span>
                </div>
              @*   <div class="form-group">
                    <label asp-for="@Model.ConfirmPassword" class="control-label" style="color:white"></label>
                    <input asp-for="@Model.ConfirmPassword" class="form-control" />
                    <span asp-validation-for="@Model.ConfirmPassword" class="text-danger"></span>
                </div> *@

                <div class="form-group">
                    <button type="submit" class="btn btn-primary">Save</button>


                </div>

            <a href="/User/Login" class="btn btn-primary">Back to Login</a>
        </form>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
